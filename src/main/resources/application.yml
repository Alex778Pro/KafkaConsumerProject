spring:
  application:
    name: KafkaConsumerProject
    kafka:
      bootstrap-servers: localhost:9092
      consumer:
        group-id: my-group
        auto-offset-reset: earliest
server:
  port: 8081